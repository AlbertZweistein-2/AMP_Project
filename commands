cmake -B build_debug -S . -DCMAKE_BUILD_TYPE=Debug
cmake --build build_debug --parallel

mpirun -np 5 ./build_debug/allgather_merge -m 4 -a 1 -t 0 -c

cmake -B build -S . -DCMAKE_BUILD_TYPE=Release
cmake --build build --parallel

mpirun -np 5 ./build/allgather_merge -c

# Docker commands
docker build -t amp_project:latest -f Dockerfile .
docker run --rm -it amp_project:latest bash

# for unit test
cd project_skeleton
docker run --rm -v $(pwd):/workspace amp_project:latest bash -c "cd /workspace && mkdir -p build-cmake && cd build-cmake && cmake .. && cmake --build ."

#TODO LIST:
# check Ex4 sum differences in benchmark with concurrent queues -> Should be fixed now -> Test!
# add free_list_insertions tracking to benchmark -> Done for Ex5
# Test mem leaks

#DONE:
# add result file name option to benchmark command line
# add max_freelist_size tracking to benchmark
# Ex5 deadlock fix in enqueue